#w: for hyprland plugin manager 
exec-once = hyprpm reload -n

windowrulev2 = float,class:(copyq)
windowrulev2 = move onscreen cursor,class:(copyq)

# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
#
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#


# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = wofi --show drun
exec-once =swww-daemon && swww img /home/sj/Downloads/muslim_couple.png --transition-type any

# Some default env vars.
env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 to 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 0
    gaps_out = 0
    border_size =2 
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # layout = dwindle
    layout = slidr

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    
    blur {
        enabled = true
        size = 9
        passes = 1
    }

# these are commented for endeavour os
    # drop_shadow = yes
    # shadow_range = 4
    # # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = no

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

# turned off for in endeavour os
# master {
#     # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
#     # new_is_master = true
# }

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER
exec-once = firefox
# exec-once = firefox dispacth workspace 1
exec-once = kitty
# exec-once = copyq&
# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive, 
bind = $mainMod,  Q, exit
# bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating, 
bind = $mainMod, D, exec, $menu
# bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, E, togglesplit, # dwindle
bind = super ALT, p,focuscurrentorlast 
bind = super ALT, f3,focuscurrentorlast 
# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d
#p: custom config
#w:  editing 
bind = $mainMod, F, fullscreen, 1
 bind = $mainMod ALT, H,  swapwindow,  l 
bind = $mainMod ALT, L, swapwindow, r
bind = $mainMod ALT, K,    swapwindow, u
bind = $mainMod ALT, J,  swapwindow, d
# bind = $mainMod ALT, M, exec,  waybar& 


 # bind = Mod4, A, exec, hyprctl swapwindow,l  && hyprctl dispatch focuscurrentorlast  

# -w: 12/11/2024 12:22 PM Tue GMT+6 Sharifpur, Gazipur, Dhaka
# resizing window 
bind = $mainMod SHIFT, l, resizeactive, 25 0
bind = $mainMod SHIFT, h, resizeactive, -25 0
bind = $mainMod SHIFT, i, resizeactive, 285 0

bind = $mainMod SHIFT, u, resizeactive, -285 0

bind = $mainMod SHIFT, k, resizeactive, 0 -25
bind = $mainMod SHIFT, j, resizeactive, 0 25

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2

bind = $mainMod, u, workspace, 1
bind = $mainMod, i, workspace, 2
bind = $mainMod, o, workspace, 3
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

#p: Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2

bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

#n  volume control 

# bind = $mainMod ALT ,I,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = $mainMod ALT ,I,  exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

bind = $mainMod ALT,U,  exec, pactl set-sink-volume @DEFAULT_SINK@ +5% 
#bind = $mainMod ALT,U,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = $mainMod, T, exec, systemctl poweroff
bind = $mainMod, Y, exec, systemctl suspend
bind = $mainMod, R, exec,  reboot
bind = $mainMod ALT, M, exec,  waybar& 
bind = $mainMod ALT, N, exec, pkill waybar
#p: reload the pipewire 
bind = $mainMod ALT, e, exec,  systemctl --user restart pipewire && systemctl --user restart pipewire-pulse && systemctl --user daemon-reload

#p: super shift p take screenshot into clipboard
bind = $mainMod Shift, p, exec, grim -g "$(slurp)" - | wl-copy --type image/png
# copy text into system clipboard from selection 
# o
bind = $mainMod Shift, o, exec ,grim -g "$(slurp)" - | convert - -density 300 -resize 200% -colorspace Gray -normalize - | tesseract stdin stdout -l eng | wl-copy
# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod Ctrl,   E, exec,  nosqlbooster&

bind = $mainMod,p , exec, google-chrome-canary 

bind = $mainMod, g, exec, espeak "biismillah" && bash -c 'sleep 300 && espeak "alhamdulillah, breath" && sleep 300 && systemctl suspend'
#--w: 12/11/2024 03:37 PM Tue GMT+6 Sharifpur, Gazipur, Dhaka
# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
bind = SUPER Ctrl, u, movetoworkspace, 1
bind = SUPER Ctrl, i, movetoworkspace, 2
bind = SUPER Ctrl, o, movetoworkspace, 3
bind = SUPER Ctrl, p, movetoworkspace, 4

bind = $mainMod, Y, exec, systemctl suspend
#p: Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
#checking fig
exec-once = wl-clipboard-history -t   
exec-once = wl-paste --watch cliphist store    
exec-once = rm "$HOME/.cache/cliphist/db"   #it'll delete history at every restart    
# include /home/sj/.config/hypr/keybindings.conf



#--w: 05/01/2025 11:11 AM Sun GMT+6 Sharifpur, Gazipur, Dhaka
# Cursor submap (similar to the Mouse mode in Sway)
submap=cursor

# Jump cursor to a position
bind=,f,exec,hyprctl dispatch submap reset && wl-kbptr -c /run/media/sj/developer/allDotfilesBackup/zshScript/config  && hyprctl dispatch submap cursor


# Cursor movement
binde=,j,exec,wlrctl pointer move 0 15
binde=, m,exec,wlrctl pointer move 0 30
binde=, return,exec,wlrctl pointer move 0 160

binde=,k,exec,wlrctl pointer move 0 -10
binde=,i,exec,wlrctl pointer move 0 -30
binde=,u,exec,wlrctl pointer move 0 -160
binde=,l,exec,wlrctl pointer move 10 0
binde=,d,exec,wlrctl pointer move 30 0
binde=, s,exec,wlrctl pointer move 160 0
binde=,h,exec,wlrctl pointer move -15 0
binde=,b,exec,wlrctl pointer move -30 0
binde=SHIFT, b,exec,wlrctl pointer move -230 0
binde=,v,exec,wlrctl pointer move -160 0


# Left button
bind=,a,exec,wlrctl pointer click left
# Middle button
# bind=,r,exec,wlrctl pointer click middle
# Right button
bind=,q,exec,wlrctl pointer click right

# Scroll up and down
# binde=,e,exec,  xdotool click 4 
# binde=,e,exec,wlrctl  xdotool click 4 pointer scroll 15 0
# binde=,w,exec, xdotool click 5

# binde=,e,exec,wlrctl pointer scroll -20 0
# binde=,w,exec,wlrctl pointer scroll 30 0
 binde=,w,exec, echo wheel -3 | dotool   # Scroll left three unitsbinde=,g,exec,wlrctl pointer scroll 0 10 
 binde=,e,exec, echo wheel 3 | dotool   # Scroll left three unitsbinde=,g,exec,wlrctl pointer scroll 0 10 

# wlrctl keyboard type "Page_Down"
# Scroll left and right
# binde=,t,exec,wlrctl pointer scroll 0 -10
binde=,t,exec, echo hwheel -3 | dotool   # Scroll left three unitsbinde=,g,exec,wlrctl pointer scroll 0 10
# binde=,t,exec, xdotool click 6
binde=,g,exec, echo hwheel 3 | dotool    # Scroll right three units 
# binde=,g,exec, xdotool click 7


bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

bind = $mainMod, F, fullscreen, 1
# Exit cursor submap
# If you do not use cursor timeout or cursor:hide_on_key_press, you can delete its respective cals
bind=,comma,exec,hyprctl keyword cursor:inactive_timeout 3; hyprctl keyword cursor:hide_on_key_press true; hyprctl dispatch submap reset 

submap = reset

# Entrypoint
# If you do not use cursor timeout or cursor:hide_on_key_press, you can delete its respective cals
bind=$mainMod,m,exec,hyprctl keyword cursor:inactive_timeout 0; hyprctl keyword cursor:hide_on_key_press false; hyprctl dispatch submap cursor

# bind = $mainMod ,   comma, exec,  hyprctl dispatch submap reset && wl-kbptr && hyprctl dispatch submap cursor
bind = $mainMod ,   comma, exec,  hyprctl dispatch submap reset && wl-kbptr -c /run/media/sj/developer/allDotfilesBackup/zshScript/config && hyprctl dispatch submap cursor

# bind = $mainMod ctrl, j,exec,wlrctl pointer move 10 0
# bind=$mainMod ctrl,k,exec,wlrctl pointer move 0 -10
# bind=$mainMod ctrl,i,exec,wlrctl pointer move 0 -30
# bind=$mainMod ctrl,u,exec,wlrctl pointer move 0 -160
# bind=$mainMod ctrl,l,exec,wlrctl pointer move 10 0
# bind=$mainMod ctrl,w,exec,wlrctl pointer move 30 0
 bind=$mainMod ctrl, q,exec,wlrctl pointer move 160 0
 bind=$mainMod ctrl,h,exec,wlrctl pointer move -15 0
 bind=$mainMod ctrl,b,exec,wlrctl pointer move -30 0
 # bind=$mainMod ctrl,v,exec,wlrctl pointer move -160 0
#

 # bind=$mainMod, N,exec,  wl-kbptr -c /mnt/fed/zshScript/config 
 

# bind = ,f4, exec, wl-kbptr -c /mnt/fed/zshScript/config

bind = ,f4, exec,  wl-kbptr -c /run/media/sj/developer/allDotfilesBackup/zshScript/config
bind =,f8, exec, wlrctl pointer click left
# wl-kbptr -C /run/media/sj/developer/allDotfilesBackup/zshScript/config


bind = SUPER ctrl, j, exec,   hints 
bind = SUPER, x, exec, hints --mode scroll
bind = $mainMod ctrl,v, exec, greenclip print | wl-copy && sleep 0.5 && rofi -modi "clipboard:greenclip print" -show clipboard -run-command 'wtype "{cmd}"'


binds {
    movefocus_cycles_fullscreen = 1
}
# Toggle the special workspace named "ferdium"
bind = ALT, X, togglespecialworkspace, ferdium


# Move the currently focused window to the special workspace "ferdium"
bind = ALT SHIFT, X, movetoworkspace, special:ferdium

